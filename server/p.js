/*const {expect, it, describe} = require('vitest');

function getCoupons () {
    let array =  [
        {
            'code': 'Noel2025', 'discount': 0.5,
            
        }
    ]

    console.log(array);

    return array;
}
getCoupons();

describe('must return an array of object', ()=> {
    it('array of object', () => {
        let expectedResult = Array;
        let typeResult = getCoupons();
        console.log(result);
        result = typeResult.values;
        expect(result).toBe(expectedResult);
    })
})*/